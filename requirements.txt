pibooth>=3.0.0
flask>=3.0.0
flask-mail>=0.9.1
qrcode[pil]>=7.4.2
Pillow>=10.0.0
netifaces>=0.11.0
imageio>=2.34.0

# AI background removal — auto-installed but only active on Pi 4+
# On Pi 3 the package installs but the feature is disabled at runtime
rembg[cpu]>=2.0.50
